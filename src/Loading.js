import React from 'react';

const Loading = () => {
  return (
    <React.Fragment>
      <div className="loading">
        <h1>loading...</h1>
      </div>
    </React.Fragment>
  );
};

export default Loading;
